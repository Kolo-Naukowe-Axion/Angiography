Local dataset storage lives here.

- `datasets/arcade/`: ignored raw and derived ARCADE assets such as `data/syntax`, `data/stenosis`, `data/vessel`, and related reports/downloads.
- `datasets/cadica/`: ignored CADICA or other extracted bbox-source data used for local prep flows.

Only lightweight docs/placeholders from this directory should be committed. Do not add dataset payloads.
